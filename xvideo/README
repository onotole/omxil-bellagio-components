The X Video component is a video display OpenMAX component that uses the Xlib
for the visualization.
To compile the component execute:

autoreconf -i -f
./configure --prefix=$INSTALL_DIR_PATH
make
make install

