The FFMpeg based OpenMAX component.

To compile the component execute:

autoreconf -i -f
./configure --prefix=$INSTALL_DIR_PATH
make
make install

This version of  ffmpeg components is compatible with
 official release of ffmpeg 0.6.1.

Actually the video decoder supports h264, the parser is under debugging.
